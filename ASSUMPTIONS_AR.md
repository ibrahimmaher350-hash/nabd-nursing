# توثيق الافتراضات والتخصيص عبر جدول Google Sheets (Tab 5) ⚙️📋

تم تصميم نظام "نبض" بمرونة عالية، حيث تعتمد المنظومة على مجموعة من الافتراضات التشغيلية والافتراضات الذكية التي تضمن عمل العيادة بسلاسة، ويمكنك تخصيص وتعديل أي منها في أي وقت مباشرة من **التاب الخامس (الإعدادات)** في جدول Google Sheets دون الحاجة لكتابة كود برمجي.

---

## 📌 جدول الافتراضات الافتراضية وكيفية تعديلها

| المفتاح في تاب الإعدادات | القيمة الافتراضية | التفسير البرمجي والتشغيلي (Assumption) | كيف تعدلها وتأثيرها على العيادة؟ |
| :--- | :--- | :--- | :--- |
| `ساعات_العمل_بداية` | `09:00` | يبدأ استقبال الزيارات والمواعيد التمريضية في تمام الساعة 9:00 صباحاً بتوقيت مصر. | لتغيير بدء العمل (مثلاً 08:00 صباحاً)، اكتب `08:00` في خانة القيمة. سيقوم نظام الحجز بمنع المواعيد الأبكر من ذلك. |
| `ساعات_العمل_نهاية` | `21:00` | ينتهي استقبال المواعيد المنزلية العادية في الساعة 9:00 مساءً (21:00). | إذا كنت تود العمل حتى 10:00 مساءً، غيّرها إلى `22:00`. |
| `تذكير_24_ساعة` | `true` | يتم جدولة تذكير آلي بالبريد الإلكتروني للعميل قبل موعد الزيارة بـ 24 ساعة. | إذا أردت إيقاف تذكير الـ 24 ساعة مؤقتاً، اكتب `false`. |
| `تذكير_1_ساعة` | `true` | يتم جدولة تذكير آلي بالبريد الإلكتروني للعميل قبل موعد الزيارة بساعة واحدة. | إذا أردت إيقاف تذكير الساعة، اكتب `false`. |
| `بريد_المالك` | `ibrahim.maher350@gmail.com` | البريد الذي يتلقى إشعارات كل حجز جديد، وهو المرتبط بحساب تقويم Google Calendar. | لتغيير بريد الاستقبال أو التقويم، اكتب البريد الجديد هنا. |
| `اسم_العيادة` | `نبض للتمريض المنزلي` | الاسم الرسمي الذي يظهر في رسائل البريد، ترويسات التقويم، ودعوات Google Meet. | اكتب أي اسم تريده وسيظهر في جميع الإشعارات. |
| `المنطقة_الزمنية` | `Africa/Cairo` | التوقيت المحلي المستخدم لحساب المواعيد وإرسال التذكيرات (توقيت القاهرة). | التوقيت المعتمد لجمهورية مصر العربية. |
| `مزامنة_تلقائية` | `true` | يتيح المزامنة اللحظية بين جدول الشيت وقاعدة بيانات Supabase. | اكتب `true` لتظل المزامنة حية ومستمرة. |

---

## 🩸 افتراضات بنك الدم (Blood Bank Assumptions)
1. **التقاط صورة سيلفي مباشرة:**
   - **الافتراض:** للحفاظ على موثوقية المتبرعين وسرعة التحقق منهم، يطلب النظام صورة سيلفي مباشرة عبر الكاميرا عند تسجيل التبرع، ولا يقبل رفع ملفات عشوائية من الجهاز.
2. **فصائل الدم المدعومة:**
   - يدعم النظام جميع فصائل الدم الثمانية (`A+`, `A-`, `B+`, `B-`, `AB+`, `AB-`, `O+`, `O-`).
   - عند طلب فصيلة نادرة مثل `O-` أو `AB-`، يقوم النظام بتمييزها تلقائياً لتسريع استجابة المتبرعين.

---

## 📅 افتراضات الحجوزات والتقويم (Calendar & Booking Assumptions)
1. **مدة الزيارة التمريضية:**
   - تُقدر مدة كل زيارة تمريضية افتراضياً بـ **60 دقيقة** في تقويم Google Calendar لمنع تداخل المواعيد.
2. **رابط اللقاء المرئي (Google Meet):**
   - يتم تلقائياً إنشاء رابط Google Meet آمن مع كل حجز جديد في تقويم جوجل في حال رغب الطبيب المشرف أو التمريض في استشارة فيديو تمهيدية مع المريض قبل التوجه لمنزله.
3. **حالات الموعد (Status Lifecycle):**
   - `scheduled` (مجدول): الموعد مؤكد وبانتظار موعد الزيارة.
   - `completed` (مكتمل): تمت الزيارة بنجاح وتم فحص المريض.
   - `cancelled` (ملغي): تم إلغاء الموعد ويتم إلغاء تذكيراته تلقائياً في قاعدة البيانات وحذف حدث التقويم.
   - `no_show` (لم يحضر): لم يتواجد المريض بالمنزل في الموعد المحدد.

---

## 🗂️ افتراضات الملف الطبي للمريض (Patient Medical Record Assumptions)
1. **تحديث الملف تلقائياً:**
   - عند حجز أي موعد برقم هاتف أو بريد مريض موجود مسبقاً، لا يتم تكرار المريض؛ بل يتم تحديث تاريخ آخر زيارة وزيادة عداد الزيارات (`visit_count + 1`).
2. **الحساسية والأمراض المزمنة:**
   - يتم تخزين الحساسيات والأدوية المزمنة كنصوص واضحة في تاب "ملفات المرضى" وتظهر فوراً أمام الممرض في شاشة الزيارة للحرص التام قبل إعطاء أي دواء أو حقنة.
