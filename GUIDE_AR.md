# دليل تشغيل وربط منصة نبض للتمريض المنزلي 🩺
### الدليل الشامل والمبسط لصاحب العيادة (خطوة بخطوة وبدون أي تعقيد)

مرحباً بك يا دكتور. هذا الدليل مخصص لك بالكامل لربط كل أجزاء موقع نبض (حسابات المستخدمين، حجز المواعيد، تقويم Google، تقارير Google Sheets، والتذكيرات التلقائية بالإيميل) لتتحول منصتك إلى **عيادة ذكية متصلة بالكامل** تعمل في خلفية النظام على مدار الساعة.

---

> [!CAUTION]
> ### 🔒 قاعدة الأمان والسرية الأولى (مهم جداً جداً):
> **لا ترسل المفاتيح السرية لأحد ولا ترفعها على GitHub أو تشاركها في محادثات عامة.**
> - المفاتيح التي تبدأ أو تحتوي على `anon` أو `public` هي مفاتيح عامة وآمنة.
> - أما المفاتيح مثل `service_role` و `client_secret` و `ENCRYPTION_KEY` فهي **سرية للغاية** ويجب حفظها فقط داخل ملف `.env.local` في جهازك أو في خوادم Vercel و Supabase.

---

## 🗺️ المرحلة 0: خريطة النظام (كيف يتكلم كل جزء مع الآخر؟)

النظام مبني ببساطة شديدة كفريق عمل متكامل:
1. **المريض يدخل الموقع (`/appointments/new` أو `/blood-bank/request`):** يحدد اسمه ورقمه ونوع الخدمة وتاريخ الزيارة (بين 9:00 ص و 9:00 م بتوقيت دمياط/القاهرة).
2. **قاعدة بيانات Supabase (المخزن الرئيسي):** تحفظ الموعد فوراً، وتُنشئ مهمتين للتذكير: تذكير قبل الزيارة بـ 24 ساعة، وتذكير آخر قبلها بساعة واحدة.
3. **تقويم Google Calendar:** يُنشئ الموعد تلقائياً في تقويم هاتفك وهاتف المريض، مع توليد رابط Google Meet تلقائي في حال كانت استشارة طبية.
4. **شيت تقرير Google Sheets:** يُضاف سطر فوري جديد ببيانات المريض وموعده وحالته، ليصبح لديك سجل حسابات ومتابعة يومي تفتحه من موبايلك في أي ثانية.
5. **خدمة Resend (الإيميل والتذكيرات):** تُرسل للمريض إيميلاً رسمياً فاخراً بهويّة نبض قبل موعده، ومعه زر مباشر: "تعديل أو إلغاء الموعد" يتيح له إدارة موعده بضغطة زر دون تعقيد.
6. **لوحة تحكمك الحية (`/dashboard`):** تفتحها فترى كل الحجوزات لحظياً (Realtime) دون حتى الحاجة لعمل تحديث للصفحة!

---

## 🛠️ المرحلة 1: تفعيل خدمات Supabase وتشغيل السكربت

مشروعك مسجل بالفعل باسم: **`nabd-blood-bank`** ومعرفه: **`obaccodbtcaxjmkaxeye`**.

### 👤 خطوات يدوية منك:
1. افتح الرابط: [https://supabase.com/dashboard](https://supabase.com/dashboard) وسجل الدخول.
2. اختر مشروعك **`nabd-blood-bank`**.
3. من القائمة الجانبية اليسرى، اضغط على **`Authentication`** (أيقونة القفل والمستخدمين) ← ثم اضغط **`Providers`** ← ثم اضغط على **`Email`**.
4. **في مرحلة التجربة الحالية:** أوقف خيار **`Confirm email`** (اجعل المفتاح غير مفعّل) حتى يتمكن المرضى وأنت من إنشاء الحساب والدخول فوراً بدون انتظار رسالة تفعيل، ثم اضغط **`Save`**.
5. من القائمة اليسرى، اضغط على **`SQL Editor`** (أيقونة الاستعلامات).
6. اضغط على الزر الأخضر **`New query`**.
7. افتح الملف المجهز لك في مشروعك: [`supabase/schema.sql`](file:///C:/Users/OS_10/Desktop/nabd-nursing/supabase/schema.sql)، انسخ كل ما بداخله بالكامل، والصقه في المربع الأسود في متصفحك.
8. اضغط الزر الأخضر في الأسفل المكتوب عليه **`Run`** (أو اضغط Ctrl+Enter).

### 👁️ ماذا يجب أن ترى؟
ستظهر لك رسالة خضراء في الأسفل: `Success. No rows returned`. ستلاحظ في القائمة اليسرى عند الذهاب إلى **`Table Editor`** وجود الجداول التالية:
- `profiles` (بيانات المستخدمين والأدوار)
- `appointments` (المواعيد والزيارات)
- `reminder_jobs` (مهام التذكير المجدولة)
- `notification_log` (سجل الإشعارات)
- `donors` و `blood_requests` و `blood_banks` (بنك الدم)

---

## 🌐 المرحلة 2: إعداد Google Cloud لربط التقويم وشيت المواعيد

### 👤 خطوات يدوية منك:
1. افتح منصة جوجل كلاود: [https://console.cloud.google.com](https://console.cloud.google.com)
2. في الشريط العلوي، اضغط على اسم المشروع واضغط **`New Project`** وسمّه: `nabd-integrations` ثم اضغط **`Create`**.
3. في شريط البحث العلوي، اكتب **`Google Calendar API`** واضغط عليها ثم اضغط الزر الأزرق **`Enable`**.
4. في شريط البحث مرة أخرى، اكتب **`Google Sheets API`** واضغط عليها ثم اضغط الزر الأزرق **`Enable`**.
5. من القائمة اليسرى (أيقونة الثلاث خطوط) ← اضغط **`APIs & Services`** ← ثم **`OAuth consent screen`**:
   - اختر **`External`** واضغط **`Create`**.
   - اسم التطبيق (App name): اكتب `نبض`.
   - إيميل الدعم (User support email): اختر إيميلك في Gmail.
   - إيميل المطور (Developer contact information): اكتب إيميلك.
   - اضغط **`Save and Continue`**.
   - في صفحة **Scopes**: اضغط **`Add or remove scopes`** وحدد:
     - `.../auth/calendar.events`
     - `.../auth/spreadsheets`
     - `.../auth/userinfo.email`
   - اضغط **`Update`** ثم **`Save and Continue`**.
   - في صفحة **Test users**: اضغط **`Add users`** وأدخل إيميلك الشخصي في Gmail ثم احفظ.
6. من القائمة اليسرى، اضغط **`Credentials`** ← ثم الزر العلوي **`+ CREATE CREDENTIALS`** ← اختر **`OAuth client ID`**:
   - نوع التطبيق (Application type): اختر **`Web application`**.
   - الاسم: `Nabd Web Client`.
   - انزل لأسفل عند **Authorized redirect URIs** واضغط **`+ ADD URI`** وأضف الرابطين التاليين بالضبط:
     - `https://nabd-nursing.vercel.app/api/auth/google/callback`
     - `http://localhost:3000/api/auth/google/callback`
   - اضغط **`Create`**.
   - ستظهر لك نافذة منبثقة بها **`Client ID`** و **`Client Secret`**. انسخهما وضع كل واحد منهما في ملف `.env.local` أمام:
     - `GOOGLE_CLIENT_ID`
     - `GOOGLE_CLIENT_SECRET`
7. **إنشاء شيت المتابعة:**
   - افتح [https://sheets.google.com](https://sheets.google.com) وأنشئ جدولاً جديداً باسم `نبض - سجل الحجوزات`.
   - في الصف الأول (الصف 1) اكتب العناوين التالية في الخانات من اليمين لليسار:
     - الخانة A: `التاريخ`
     - الخانة B: `الوقت`
     - الخانة C: `اسم المريض`
     - الخانة D: `الهاتف`
     - الخانة E: `نوع الزيارة`
     - الخانة F: `الحالة`
     - الخانة G: `رابط الموعد`
     - الخانة H: `معرف الحجز`
   - انسخ كود الشيت من شريط العنوان في المتصفح (هو النص الطويل الموجود بين `/d/` و `/edit`).
   - الصقه في ملف `.env.local` أمام `GOOGLE_SHEET_ID`.
8. الآن افتح الرابط: `http://localhost:3000/api/auth/google` (أو من لوحة التحكم) وسجل الدخول بحسابك في Google، ووافق على الصلاحيات.

### 👁️ ماذا يجب أن ترى؟
سيتم تحويلك تلقائياً إلى لوحة التحكم مع شريط أخضر: **"تم ربط حساب Google (Calendar & Sheets) بنجاح!"**.

---

## 📧 المرحلة 3: تفعيل خدمة Resend لإرسال إيميلات التذكير

### 👤 خطوات يدوية منك:
1. افتح الموقع: [https://resend.com](https://resend.com) واضغط **`Sign Up`** وسجل دخولك بحسابك في GitHub أو بإيميلك مجاناً.
2. من القائمة الجانبية اليسرى، اضغط على **`API Keys`**.
3. اضغط الزر الأسود **`Create API Key`** وسمّه `nabd-reminders`.
4. انسخ المفتاح الذي يبدأ بـ `re_`.
5. افتح ملف `.env.local` والصق المفتاح أمام `RESEND_API_KEY`.
6. في نفس الملف، تأكد أن:
   - `EMAIL_FROM="نبض للتمريض المنزلي <onboarding@resend.dev>"`
   - `OWNER_EMAIL=اكتب_ايميلك_هنا`

---

## ⚡ المرحلة 4: نشر دوال Supabase Edge Functions (عقل الأتمتة)

قمنا ببرمجة 3 دوال Deno سحابية كاملة داخل مجلد `supabase/functions/`:
1. `on-new-appointment`: تعمل فور حجز أي موعد لإضافته في تقويم Google وتحديث الشيت.
2. `process-reminders`: تعمل دورياً للتحقق من المواعيد وإرسال إيميلات التذكير (24 ساعة وساعة).
3. `on-appointment-update`: تعمل عند قيامك أو قيام المريض بتعديل أو إلغاء الموعد لتعديل تقويم Google وإيقاف التذكيرات.

### 👤 خطوات النشر (من التيرمينال):
إذا كان لديك Supabase CLI، نفذ الأوامر التالية لنشر الدوال بضغطة زر:
```bash
# 1. تسجيل الدخول إلى سوبابيز
npx supabase login

# 2. ربط المشروع
npx supabase link --project-ref obaccodbtcaxjmkaxeye

# 3. ضبط المتغيرات السرية في السحابة
npx supabase secrets set RESEND_API_KEY="مفتاح_ريسيند" GOOGLE_CLIENT_ID="معرف_جوجل" GOOGLE_CLIENT_SECRET="سر_جوجل" ENCRYPTION_KEY="c3ab8ff13720e8ad9047dd39466b3c8974e592c2fa383d4a3960714caef0c4f2" JWT_SECRET="nabd-clinic-super-secret-jwt-key-2026-cairo-secure-tokens"

# 4. نشر الدوال
npx supabase functions deploy on-new-appointment --no-verify-jwt
npx supabase functions deploy process-reminders --no-verify-jwt
npx supabase functions deploy on-appointment-update --no-verify-jwt
```

> [!TIP]
> **ملاحظة ذكية مدمجة في نبض:**
> حتى لو لم تقم بنشر Edge Functions الآن، فإن كود الموقع في مسارات Next.js (`/api/appointments` و `/api/cron/reminders`) يتولى المزامنة مع تقويم Google وشيت الحسابات تلقائياً ومباشرة!

---

## 🧪 المرحلة 5: سيناريو الاختبار الشامل للتأكد من عمل كل شيء (End-to-End Test)

قم بتنفيذ هذه الخطوات البسيطة لتتأكد بنفسك:
1. **احجز موعد تجربة:**
   - افتح الصفحة: [http://localhost:3000/appointments/new](http://localhost:3000/appointments/new)
   - اكتب اسمك، هاتفك، وإيميلك الشخصي.
   - اختر موعداً بعد ساعتين من الآن، واضغط **"تأكيد حجز الزيارة المنزلية 🩺"**.
2. **تحقق من التقويم والشيت:**
   - افتح تطبيق **Google Calendar** على موبايلك ← ستجد موعداً مسجلاً باسم "نبض: زيارة تمريضية - اسمك".
   - افتح ملف **Google Sheets** ← ستجد سطراً جديداً ظهر فوراً ببياناتك.
3. **تحقق من لوحة التحكم:**
   - افتح [http://localhost:3000/dashboard](http://localhost:3000/dashboard) ← ستجد الموعد ظاهراً مع عدادات اليوم وتذكيراته.
4. **جرّب التعديل والإلغاء:**
   - اضغط زر **"تعديل"** في لوحة التحكم أو من خلال صفحة إدارة الموعد، غيّر الوقت واضغط حفظ ← ستجد تقويم Google تم تعديله تلقائياً!
   - اضغط **"إلغاء الموعد"** ← ستجد الموعد حُذف من تقويم Google وتغيرت حالته إلى "ملغي" وتوقفت تذكيراته.
5. **جرّب طلب الدم مع موعد:**
   - ادخل على `/blood-bank/request` واطلب فصيلة دم مع تفعيل خيار **"أريد تحديد موعد للتبرع ومرافقة تمريضية"** ← سيظهر طلب الدم في بنك الدم، وسيظهر موعد الزيارة أيضاً في جدول المواعيد وتقويم Google!

---

## 🩺 جدول حل المشاكل الشائعة (المشكلة / الحل)

| المشكلة | السبب | الحل البسيط |
| :--- | :--- | :--- |
| **invalid_grant أو انتهت صلاحية الرمز** | جلسة Google انتهت أو تم تغيير كلمة السر | ادخل على `/api/auth/google` وسجل الدخول مرة واحدة فقط لتجديد الرمز المشفر. |
| **permission denied for table appointments** | لم يتم تطبيق الصلاحيات في سوبابيز | افتح SQL Editor في Supabase والصق السكربت من جديد وتأكد من تشغيله بنجاح. |
| **الإيميل لم يصل إلى صندوق الوارد** | في حساب Resend المجاني التجريبي، تصل الإيميلات للإيميل المسجل فقط أو تفقد مجلد Spam | افتح لوحة تحكم [Resend Logs](https://resend.com/emails) لرؤية حالة الإرسال بدقة، وتأكد من إيميل المرسل إليه. |
| **تكرار الموعد في التقويم مرتين** | محاولة إنشاء متزامنة | الدوال محمية بخاصية Idempotency وتفحص `google_event_id` قبل الإضافة لمنع أي تكرار. |
| **الموعد خارج ساعات العمل** | المريض اختار وقتاً قبل 9 ص أو بعد 9 م | النظام يُلزم المريض بأوقات العمل الرسمية لنبض (9:00 ص إلى 9:00 م بتوقيت القاهرة). |

---

**تهانينا! منصة نبض الآن جاهزة تماماً وتعمل كمنظومة عيادة آلية ومحترفة.**
